<% if @must_login %>
  <div class="need_login">
    <p>You need to Sign Up or Sign In to contine </p>
  </div>
<% else %>
  <div class="container-fluid index_search_col">
    
     
      <div class="search-wrapper">
        <%= render "search" %>
      </div>
    
  </div>

  
  <div class="row all-widgets">
    <%= render partial: "widget", collection: @widgets %>
  </div>
<% end %>
